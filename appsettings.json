{
  "Logging": {
    "LogLevel": {
      // BUG: There is a log message from "Microsoft.AspNetCore.Components.Server.ComponentHub[6]" that prints
      // a InvalidOperationException about the unresolved dependency. However, it is of log severity 'Debug'. I think
      // it should be higher severity!
      "Default": "Information"
    }
  },
  "AllowedHosts": "*"
}
